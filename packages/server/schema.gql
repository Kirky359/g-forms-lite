# ------------------------------------------------------
# THIS FILE WAS AUTOMATICALLY GENERATED (DO NOT MODIFY)
# ------------------------------------------------------

type AnswerEntity {
  questionId: ID!
  value: String!
}

input AnswerInputDto {
  questionId: ID!
  value: String!
}

type FormEntity {
  description: String
  id: ID!
  questions: [QuestionEntity!]!
  requireEmail: Boolean!
  title: String!
}

type Mutation {
  createForm(description: String, questions: [QuestionInputDto!], requireEmail: Boolean, title: String!): FormEntity!
  deleteForm(id: ID!): Boolean!
  submitResponse(answers: [AnswerInputDto!]!, formId: ID!, respondentEmail: String): ResponseEntity!
  updateForm(description: String, id: ID!, questions: [QuestionInputDto!]!, requireEmail: Boolean, title: String!): FormEntity!
}

type Query {
  form(id: ID!): FormEntity
  forms: [FormEntity!]!
  responses(formId: ID!): [ResponseEntity!]!
}

type QuestionEntity {
  correctAnswer: String
  id: ID!
  options: [String!]
  required: Boolean
  text: String!
  type: QuestionType!
}

input QuestionInputDto {
  correctAnswer: String
  options: [String!]
  required: Boolean
  text: String!
  type: QuestionType!
}

"""Type of form question"""
enum QuestionType {
  CHECKBOX
  DATE
  EMAIL
  MULTIPLE_CHOICE
  TEXT
}

type ResponseEntity {
  answers: [AnswerEntity!]!
  formId: ID!
  id: ID!
  respondentEmail: String
  score: Int
}