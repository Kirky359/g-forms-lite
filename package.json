{
  "name": "google-forms-lite",
  "private": true,
  "workspaces": ["packages/*"],
  "scripts": {
    "dev": "npm run build --workspace=@forms/shared && concurrently \"npm run dev --workspace=server\" \"npm run dev --workspace=client\"",
    "dev:server": "npm run dev --workspace=server",
    "dev:client": "npm run dev --workspace=client",
    "build": "npm run build --workspaces",
    "typecheck": "npm run typecheck --workspaces --if-present"
  },
  "devDependencies": {
    "concurrently": "^9.1.0"
  }
}
